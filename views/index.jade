extends layout

block content
	- var className = 'item '+rows+'rows '+columns+'columns'
	div#main
		each item in items
			div(class=className)
				h2= item.summary
				h3
					span= item.weekDay
					span= item.dateStr